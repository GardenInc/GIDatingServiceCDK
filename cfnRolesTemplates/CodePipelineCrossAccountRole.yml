AWSTemplateFormatVersion: 2010-09-09

Description: The AWS CloudFormation template that creates the CodePipeline
  Cross Account Role that will be assumed by the pipeline account to deploy
  into target accounts

Parameters:
  PipelineAccountID:
    Description: Account ID of the pipeline AWS Account that initiates deployments to this account.
    Type: String
    ConstraintDescription: Must be a valid AWS Account ID without hyphens.
    AllowedPattern: '\d{12}'
    MinLength: 12
    MaxLength: 12
  Stage:
    Description: Stage of the account we're deploying to
    Type: String
    AllowedValues:
      - Prod
      - Beta
  KeyArn:
    Description: Provide the KMS Key ARN if you've deployed the pipeline stack
    Type: String
    ConstraintDescription: Must be a valid AWS ARN for a KMS Key in the PIPELINE account
    Default: ''
  FrontEndKeyArn:
    Description: Provide the KMS Key ARN if you've deployed the front end pipeline stack
    Type: String
    ConstraintDescription: Must be a valid AWS ARN for a KMS Key in the PIPELINE account
    Default: ''

# If the Key Arn is blank, it's our initial deployment,
# so don't deploy the policy which references the bucket and key
Conditions:
  DeployPolicy: !Not [!Equals [!Ref FrontEndKeyArn, '']]

Resources:
  CrossAccountDeploymentRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: CodePipelineCrossAccountRole
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Principal:
              AWS:
                - !Sub arn:${AWS::Partition}:iam::${PipelineAccountID}:root
            Action:
              - sts:AssumeRole

  CrossAccountDeploymentPolicy:
    Condition: DeployPolicy
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Allows pipeline in master account to deploy API Gateway, Lambda
      ManagedPolicyName: CodePipelineCrossAccountPolicy
      Roles:
        - !Ref CrossAccountDeploymentRole
      PolicyDocument:
        Version: 2012-10-17
        Statement:
          - Action:
              - cloudformation:CreateStack
              - cloudformation:GetTemplate
              - cloudformation:ValidateTemplate
              - cloudformation:DeleteStack
              - cloudformation:UpdateStack
              - cloudformation:DescribeStack*
            Resource: !Sub arn:${AWS::Partition}:cloudformation:${AWS::Region}:${AWS::AccountId}:stack/*
            Effect: Allow
          - Action:
              - s3:*
            Resource: !Sub arn:${AWS::Partition}:s3:::*
            Effect: Allow
          - Action:
              - iam:PassRole
            Resource: !Sub arn:${AWS::Partition}:iam::${AWS::AccountId}:role/CloudFormationDeploymentRole
            Effect: Allow
          - Action:
              - s3:*
            Resource:
              - !Sub arn:${AWS::Partition}:s3:::*
            Effect: Allow
          - Action:
              - kms:DescribeKey
              - kms:GenerateDataKey*
              - kms:Encrypt
              - kms:ReEncrypt*
              - kms:Decrypt
            Resource:
              - !Sub ${KeyArn}
              - !Sub ${FrontEndKeyArn}
            Effect: Allow
